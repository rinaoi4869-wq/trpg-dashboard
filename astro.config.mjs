import { defineConfig } from 'astro/config';

export default defineConfig({
  // あなたのGitHub PagesのURLの「大元」を指定
  site: 'https://rinaoi4869-wq.github.io',
  
  // あなたのリポジトリ名を指定（/ から書き始めてください）
  base: '/trpg-dashboard',
});